# 2FA-Unterstützung Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TOTP-basierte 2FA beim Login unterstützen — OTP-Feld erscheint automatisch wenn der Server es fordert.

**Architecture:** `fetchToken` bekommt einen optionalen `otp`-Parameter. Bei 400/401 mit OTP-Keyword wirft die API einen neuen `otpRequired`-Fehler. `LoginView` fängt diesen ab und blendet ein OTP-Textfeld ein; beim zweiten Versuch wird der Code mitgeschickt.

**Tech Stack:** SwiftUI, URLSession, existing `PaperlessAPI` / `LoginView`

---

## File Map

| Datei | Status | Verantwortung |
|-------|--------|---------------|
| `Paperless TeDi/Services/PaperlessAPI.swift` | Ändern | `APIError.otpRequired`, `fetchToken(otp:)`, OTP-Keyword-Erkennung |
| `Paperless TeDi/Views/Auth/LoginView.swift` | Ändern | `otpRequired`-State, OTP-Feld, angepasster Login-Flow |

---

## Task 1: APIError.otpRequired + fetchToken mit OTP-Parameter

**Files:**
- Modify: `Paperless TeDi/Services/PaperlessAPI.swift:3-83`

- [ ] **Schritt 1: `APIError.otpRequired` hinzufügen**

Ersetze den gesamten `APIError`-Block (Zeilen 3–19) durch:

```swift
enum APIError: Error, LocalizedError {
    case invalidURL
    case unauthorized
    case otpRequired
    case serverError(Int)
    case noData
    case decodingError(Error)

    var errorDescription: String? {
        switch self {
        case .invalidURL:           return "Ungültige URL"
        case .unauthorized:         return "Nicht autorisiert (401)"
        case .otpRequired:          return "2FA-Code erforderlich"
        case .serverError(let c):   return "Server Fehler: \(c)"
        case .noData:               return "Keine Daten erhalten"
        case .decodingError(let e): return "Datenfehler: \(e.localizedDescription)"
        }
    }
}
```

- [ ] **Schritt 2: `fetchToken` mit `otp`-Parameter und OTP-Erkennung ersetzen**

Ersetze die gesamte `fetchToken`-Methode (Zeilen 64–83) durch:

```swift
static func fetchToken(
    serverUrl: String,
    username: String,
    password: String,
    otp: String? = nil
) async throws -> String {
    var clean = serverUrl.trimmingCharacters(in: .whitespacesAndNewlines)
    if clean.hasSuffix("/") { clean.removeLast() }
    let prefix = clean.lowercased().hasPrefix("http") ? "" : "http://"
    guard let url = URL(string: "\(prefix)\(clean)/api/token/") else { throw APIError.invalidURL }

    var req = URLRequest(url: url)
    req.httpMethod = "POST"
    req.setValue("application/json", forHTTPHeaderField: "Content-Type")
    var body: [String: String] = ["username": username, "password": password]
    if let otp { body["otp"] = otp }
    req.httpBody = try? JSONSerialization.data(withJSONObject: body)

    let (data, response) = try await URLSession.shared.data(for: req)
    guard let http = response as? HTTPURLResponse else { throw APIError.noData }

    if (400...401).contains(http.statusCode) {
        let raw = String(data: data, encoding: .utf8)?.lowercased() ?? ""
        let otpKeywords = ["otp", "totp", "mfa", "2fa", "one-time"]
        if otpKeywords.contains(where: { raw.contains($0) }) {
            throw APIError.otpRequired
        }
        throw APIError.unauthorized
    }
    if !(200...299).contains(http.statusCode) { throw APIError.serverError(http.statusCode) }

    guard let json = try? JSONSerialization.jsonObject(with: data) as? [String: Any],
          let token = json["token"] as? String else { throw APIError.noData }
    return token
}
```

- [ ] **Schritt 3: Build prüfen**

In Xcode: `⌘B` — keine Fehler erwartet. Der Compiler wird alle `switch`-Statements auf `APIError` prüfen; falls irgendwo ein exhaustive-switch-Fehler erscheint, den neuen `case .otpRequired` dort ergänzen.

- [ ] **Schritt 4: Commit**

```bash
git add "Paperless TeDi/Services/PaperlessAPI.swift"
git commit -m "feat: APIError.otpRequired und fetchToken mit OTP-Parameter"
```

---

## Task 2: LoginView — OTP-State und angepasster Flow

**Files:**
- Modify: `Paperless TeDi/Views/Auth/LoginView.swift`

- [ ] **Schritt 1: Zwei neue State-Variablen hinzufügen**

In `LoginView`, direkt nach `@State private var errorMessage = ""`:

```swift
@State private var otpRequired = false
@State private var otpCode = ""
```

- [ ] **Schritt 2: OTP-Feld in den Body einfügen**

Im `body`-VStack, direkt nach dem `SecureField("Passwort", ...)` und vor dem `Toggle("FaceID", ...)`:

```swift
if otpRequired {
    VStack(spacing: 4) {
        Text("Gib deinen Authentifikator-Code ein")
            .font(.caption)
            .foregroundColor(.secondary)
        TextField("6-stelliger Code", text: $otpCode)
            .textFieldStyle(.roundedBorder)
            .keyboardType(.numberPad)
            .multilineTextAlignment(.center)
    }
}
```

- [ ] **Schritt 3: Button-Label anpassen**

Den bestehenden Login-Button:
```swift
Button("Login") { login() }.buttonStyle(.borderedProminent)
```
ersetzen durch:
```swift
Button(otpRequired ? "Code bestätigen" : "Login") { login() }
    .buttonStyle(.borderedProminent)
```

- [ ] **Schritt 4: `login()`-Funktion ersetzen**

Die gesamte `login()`-Methode ersetzen durch:

```swift
private func login() {
    isChecking = true
    errorMessage = ""
    Task {
        do {
            if !otpRequired {
                try await PaperlessAPI.checkConnection(
                    serverUrl: store.serverUrl,
                    username: store.username,
                    password: password
                )
            }
            let token = try await PaperlessAPI.fetchToken(
                serverUrl: store.serverUrl,
                username: store.username,
                password: password,
                otp: otpRequired ? otpCode : nil
            )
            KeychainService.saveToken(
                token.trimmingCharacters(in: .whitespacesAndNewlines),
                for: store.serverUrl
            )
            store.isDemoMode = false
            onConnect()
        } catch APIError.otpRequired {
            otpRequired = true
            errorMessage = ""
        } catch APIError.unauthorized {
            errorMessage = otpRequired ? "Falscher Code" : "Login fehlgeschlagen"
        } catch {
            errorMessage = error.localizedDescription
        }
        isChecking = false
    }
}
```

- [ ] **Schritt 5: Build prüfen**

`⌘B` — keine Fehler erwartet.

- [ ] **Schritt 6: Commit**

```bash
git add "Paperless TeDi/Views/Auth/LoginView.swift"
git commit -m "feat: 2FA OTP-Feld in LoginView"
```

---

## Task 3: Manuell testen

- [ ] **Schritt 1: Login ohne 2FA testen**

App starten → normaler Login mit Benutzer ohne 2FA aktiviert → kein OTP-Feld erscheint → Login klappt.

- [ ] **Schritt 2: Login mit 2FA testen (falls verfügbar)**

Login mit einem Benutzer, der 2FA aktiviert hat:
1. Username + Passwort eingeben → Login tippen
2. OTP-Feld erscheint mit Hinweistext, Button zeigt „Code bestätigen"
3. 6-stelligen Code aus Authenticator-App eingeben → „Code bestätigen" tippen
4. App öffnet sich normal

- [ ] **Schritt 3: Falschen Code testen**

OTP-Feld ausgefüllt → bewusst falschen Code eingeben → „Falscher Code" erscheint, OTP-Feld bleibt sichtbar.

- [ ] **Schritt 4: Finaler Commit**

```bash
git add -A
git commit -m "feat: 2FA-Unterstützung vollständig implementiert"
```

---

## Self-Review Checkliste

- [x] `APIError.otpRequired` definiert mit `errorDescription`
- [x] `fetchToken` hat `otp: String? = nil` — bestehende Aufrufe ohne OTP bleiben kompatibel
- [x] 400 und 401 werden beide auf OTP-Keywords geprüft
- [x] Keywords: `"otp"`, `"totp"`, `"mfa"`, `"2fa"`, `"one-time"` (case-insensitive via `.lowercased()`)
- [x] `checkConnection` wird beim OTP-Versuch übersprungen (TOTP-Code ist zeitkritisch)
- [x] OTP-Feld hat `.keyboardType(.numberPad)`
- [x] Button-Label wechselt zu „Code bestätigen" wenn OTP-Feld sichtbar
- [x] Falscher Code zeigt „Falscher Code" ohne Screen-Reset
- [x] Kein neuer Error-Case in `checkConnection` nötig (gibt kein Token zurück)
